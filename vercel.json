{
    "builds": [
      {
        "src": "app.py",
        "use": "@vercel/python",
        "config": {
          "includeFiles": ["requirements.txt", "build.sh"],
          "installCommand": "./build.sh && pip install -r requirements.txt"
        }
      }
    ]
  }
  